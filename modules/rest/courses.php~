<?php
function GetCourses() {
    $database = Database::get();
    $database->queryFunc(
	'SELECT title FROM course',
	function ($row) use (&$courses) {$courses[] = var_dump($row);}
	);
	echo json_encode($courses);
}
function PostCourses() {
    echo 'POST';
}
function DeleteCourses() {
    echo 'DELETE';
}
?>
